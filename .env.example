# Database connection URL for PostgreSQL
# This should match the database configuration in docker-compose.yaml
DATABASE_URL=postgresql://postgres:password@localhost:54321/electric

# Authentication secret - REQUIRED in production
# Generate a strong secret (minimum 32 characters)
BETTER_AUTH_SECRET=

# GitHub OAuth - REQUIRED for GitHub authentication
# Create a GitHub OAuth app at https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# GitHub Webhook configuration for PR event sync
# GITHUB_WEBHOOK_URL: The public URL where GitHub will send webhook events
# Example: https://your-app.com/api/github-webhook
GITHUB_WEBHOOK_URL=

# GITHUB_WEBHOOK_SECRET: Signing secret for webhook verification
# Generate a random string and configure the GitHub webhook to use it
GITHUB_WEBHOOK_SECRET=

# Production Electric Cloud Configuration (optional)
# Get these from https://dashboard.electric-sql.cloud/
# ELECTRIC_SOURCE_ID=your-source-id
# ELECTRIC_SOURCE_SECRET=your-source-secret
